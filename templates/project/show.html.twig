{% extends 'project/administrateur_layout.twig' %}

{% block title %}Project{% endblock %}

{% block contenue %}
    {{ parent() }}
<div class="col">
    <div class="main">

        <p class="titre">Project</p>
<br>

        <table class="table table-hover" style="font-family: 'Dancing Script', cursive;font-size: 15px">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ project.id }}</td>
            </tr>
            <tr>
                <th>Nom_projet</th>
                <td>{{ project.nomProjet }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ project.description }}</td>
            </tr>
            </tbody>
        </table>

    <div class="row">
        <div class="col-lg-6 col-md-6 col-sm-12">
            <a href="{{ path('project_index') }}"><button type="button" class="btn btn-outline-success">retourner à la liste</button></a>

        </div>

        <div class="col-lg-6 col-md-6 col-sm-12">
            <a href="{{ path('project_edit', {'id': project.id}) }}"><button type="button" class="btn btn-outline-info">editer</button></a>

        </div>

    </div>


    </div>
    </div>
    </div>
    <br>
{% endblock %}

