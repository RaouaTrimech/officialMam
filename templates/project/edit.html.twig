{% extends 'project/administrateur_layout.twig' %}

{% block title %}Edit Project{% endblock %}

{% block contenue %}
    {{ parent() }}
    <div class="col">
        <div class="main">

            <p class="titre">Edit Project</p>

            {{ include('project/_form.html.twig', {'button_label': 'Update'}) }}
            <div class="row">
                <div class="col align-self-center">
                    <button type="button" class="btn btn-outline-danger"><a href="{{ path('project_index') }}">retour à la liste</a></button>
                </div>
            </div>

        </div>

    </div>
    </div>
    <br>
{% endblock %}
